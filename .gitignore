# clasp 設定（scriptId を含むため除外）
.clasp.json

# macOS
.DS_Store

# エディタ
.vscode/
*.swp

# env files: real values are never committed (dotenvx-encrypted files are added explicitly)
.env
.env.*
!.env.example
